# Render: app reads HOST (default 0.0.0.0) and PORT (set by Render) from env
services:
  - type: web
    name: ticket-rag-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python api.py
    envVars:
      - key: GEMINI_API_KEY
        sync: false
